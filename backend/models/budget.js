/* Purpose: 
    To allow users to set budgets for different categories
    and track their spending against these budgets.

    Fields:

    Category (Reference to Category model),
    Amount (Number),
    Period (String or Date Range),
    User (Reference to User model). 
*/