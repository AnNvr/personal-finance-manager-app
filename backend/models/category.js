/*  Purpose:
    To categorize transactions (e.g., groceries, utilities, entertainment).
    This allows users to filter transactions and view spending by categories.

    Fields:
    
    Name (String),
    Type (Enum: ['Income', 'Expense']),
    User (Reference to User model for personalization),
    Icon (String for UI representation).
*/