/* 
    Purpose: 
    To enable users to set financial goals (e.g., saving for a vacation, paying off debt).

    Fields: 
    Name (String),
    TargetAmount (Number),
    CurrentAmount (Number, default to 0),
    Deadline (Date),
    User (Reference to User model). */